import React from "react";

const Nav = () => {
    return (
        <div>

            {/* Logo Image, which links to Home route */}

            {/* App Title, which links to Home route */}

            {/* About, which links to About route */}

            {/* Contact, which links to Contact route */}

            {/* Placeholder text */}
            <p>Nav</p>
        </div>
    );
}

export default Nav;